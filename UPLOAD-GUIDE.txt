更新順序：

1. Cloudflare Worker → Edit code
   用 cloudflare-worker.js 全部覆蓋 → Deploy。

2. 保留目前 MARKET_DATA → stock-market-data binding。

3. 保留目前 Cron：30 0 * * *。

4. GitHub stock-return-backtester 根目錄：
   用新版 index.html 覆蓋舊檔並 Commit。

5. 測試 Worker：
   /health
   /benchmark-chart?symbol=SPY&start=2020-01-01&end=2026-08-09
   /chart?symbol=GOOGL&start=2020-01-01&end=2026-08-09

預期：
- benchmark-chart 回傳 cacheSource = KV。
- chart 不回 cacheSource=KV，且每次直接向 Yahoo 查主標的。
- 網頁不再顯示資料來源 footer。
